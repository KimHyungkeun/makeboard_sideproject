<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sideproject.makeboard.mapper.BoardMapper">

    <select id="getBoardInfo" resultType="java.util.HashMap">
        SELECT id, title, nickname, update_date
        FROM baseboardlist
        ORDER BY id
        OFFSET #{offset}
        LIMIT #{listCnt};
    </select>

    <select id="getBoardInfoWithId" resultType="com.sideproject.makeboard.dto.BoardInfo">
        SELECT nickname, title, content, update_date
        FROM baseboardlist
        WHERE 1=1 AND id = #{id}
        LIMIT 1;
    </select>


</mapper>